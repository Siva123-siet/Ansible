- name: Install nginx
  hosts: dynamic_frontend
  become: yes
  tasks:
  - name: Install nginx
    ansible.builtin.dnf:
     name: nginx
     state: Installed

  - name: run nginx
    ansible.builtin.service:
     name: nginx
     state: started
     enabled: yes